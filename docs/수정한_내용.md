# 수정한 내용

개발하면서 주고받은 대화 전문은 [개발_기록.md](개발_기록.md) 에 있다 (`python tools/export_devlog.py` 로 갱신).

큰 변화나 아예 새로운 기능은 **새 브랜치**로 올린다.
그 밖의 수정 — 버그 고침, 화면 다듬기, 문구 손질 — 은 **기존 브랜치에 올리고 이 파일에 날짜·시간별로 적는다.**
날짜는 `## YYYY-MM-DD`, 그 아래 항목은 `### HH:MM — 제목` (한국 시간) 으로 쓴다. 최신이 위로 온다.
올릴 때는 **지정된 브랜치에만** 올린다 — PR 은 만들지 않고, 다른 브랜치로는 보내지 않는다.

| 브랜치 | 무엇 |
|---|---|
| `김준영_20260905_2337` | 게임 최초 구현 (증강 로그라이트 체스) |
| `김준영_20260907_1303` | Montage(WDS) 디자인 시스템 적용 · UI 재배치 |
| `김준영_20260909_1719` | **온라인 실시간 대전** (방 코드) |
| `김준영_20260910_1453` | 대화 · 증강 제한시간 · 판 읽기 · 방 설정 |
| `김준영_20260910_1740` | **대국 통계 수집** (수퍼베이스) |
| `김준영_20260912_1924` | 마지막 수 표시 · 증강 창 순서 · 문구 손질 · 문서 정리 |
| `김준영_20260914_1608` | **계정 — 로그인하면 전적이 이어짐** · 전적 저장 스위치 · **수 예약** · 상대 증강 선택 표시 · 온라인 증강 알림·발동 손질 |
| `김준영_20260917_1338` | **피드백 10건 전수 점검** — 도감·규칙 화면 정리 · 포영/지정불가 회귀 테스트 · `K1b` 는 그 칸의 3개 중 2개 — 지금 작업 중인 브랜치 |

---

## 2026-09-17

### 16:18 — 답변 마무리 규칙 (`CLAUDE.md`)

배포 주소만 맨 밑에 덩그러니 붙이던 것을 고쳤다. 이제 **맨 끝 한 번만**, 두 곳을 링크로 건다.

- 올린 곳 — 포크 브랜치 목록 <https://github.com/Argenviol/20260914_1608/branches>
- 해 보기 — <https://unnamed-chess.onrender.com/>

주소만 적지 않고 **무슨 브랜치에 무엇을 올렸는지 · 가서 무엇을 보면 되는지**를 링크에 얹어 한 줄로 쓴다.
중간에 또 적지 않는다.

### 16:06 — `피드백.md` 에 처리 표시

지금까지는 원문 보존용으로 받은 그대로만 뒀는데, 처리된 항목도 그대로 남아 있어 무엇이 아직인지 알 수 없었다.
**받은 글은 한 글자도 바꾸지 않고** 줄 끝에 표시와 짧은 설명만 붙였다.

- ✅ **고침** — 코드를 바꾼 것: 시작화면 도감·규칙, 도감 카드 높이, `K1b`, '판을 보는 중입니다' 크기,
  규칙 바로가기, 전역 룰 접기, 드래프트 설명, 폰 두 칸 전진, 비숍 `B3c` 세 가지
- 🔎 **확인** — 돌려 봤더니 이미 정상이라 테스트로 잠근 것: 증강 색 구분, 포영/지정불가

항목마다 무엇이 문제였고 어떻게 고쳤는지, 언제 적어 둔 건지(이 문서의 날짜·시간)를 한두 줄로 달았다.
머리말에는 `node tools/test/feedback.js` 로 전부 화면에서 다시 돌려 볼 수 있다고 적었다.

### 15:58 — 피드백 전 항목을 '실제 화면' 에서 확인 (`tools/test/feedback.js`, 48항목)

`K1b` 가 코드만 읽어서는 안 보였던 것처럼, 나머지도 화면에서 직접 돌려 봤다.

기존 `augments.js` 는 `Game.api` 를 가짜로 바꿔 훅만 본다 — 증강이 판을 바꾸는지는 알지만
**사람이 화면에서 겪는 것**(칸에 점이 찍히는지 · 배지가 붙는지 · 선택지가 잠겨서 오는지)은 못 본다.
새로 넣은 `feedback.js` 는 판만 차려 놓고 그 뒤로는 전부 진짜다 — **진짜 칸을 눌러서 두고,
진짜 드래프트 창의 카드를 누르고, 진짜 대상 지정 바에서 칸을 찍는다.** `Game.api` 는 손대지 않는다.

```
python server/relay.py
node tools/test/feedback.js      # 48항목
```

| 피드백 | 화면에서 확인한 것 |
|---|---|
| 시작화면에 도감·규칙이 없다 | 메인에서 두 창이 열리고, 판에서도 📖 ❓ 로 열린다 |
| 도감 가독성 | 기물 '킹' 을 누르면 12장, '1개' 를 누르면 3장. 한 기물 12장이 한 화면에 (본문 높이 ≤ 보이는 높이 × 1.1) |
| 규칙/용어 UI | 바로가기 네 개 · '용어' 를 누르면 실제로 그 자리로 내려간다(scrollTop > 100) |
| 전역 룰 밑 설명 | 열었을 때 펼쳐진 줄 0개, 눌러야 본문이 나온다 |
| 드래프트 방식 설명 | 폰으로 처치하면 머리말이 '폰 증강', 카드가 `P1a P1b P1c` 셋 |
| 증강 색 구분 | 세 장이 각각 `d-turn` · `d-count` · `d-perm`, 범례도 함께 |
| '판을 보는 중입니다' | 실제 계산된 글씨 크기 제목 22px · 버튼 18px 이상, 덮개가 걷히고 기물이 그대로 보인다 |
| **지정불가** | `P1a` 로 a2 폰을 찍으면 배지가 붙고, **흑 룩을 눌렀을 때 a2 가 잡을 칸으로 안 찍힌다**(a3 은 찍힌다). 상대턴이 지나면 배지가 사라지고 다시 잡힌다 |
| **포영** | `P1c` 로 두 기물이 판에서 사라지고 칩 2개. 흑 룩이 그 칸으로 들어갈 수 있고, 돌아온 폰이 **그 룩을 밀어낸다**. 두 수 뒤 제자리 복귀 |
| 폰 두 칸 전진 | `P1b` 를 고른 뒤 기본 배치 폰을 누르면 점이 `e3 e4 e5`, 이미 움직인 폰은 `g5 g6` |
| 비숍 '비밀' `B3c` | 처치한 그 비숍으로 **백의 두 번째 차례에** 부활 선택이 뜨고, b1·g1 이 막힌 나이트는 **잠긴 채로** 온다. 룩을 골라 a1 에 되살아난다 |
| `K1b` | 킹으로 처치 → 킹 1개 칸에서 `K1b`, 이어지는 3개 티어가 **같은 킹 칸을 두 번**. 두 번째에는 방금 고른 카드가 잠겨서 온다 |

확인하다 알게 된 것 — `B3c` 의 '2턴' 은 **처치한 수(백 T0) 뒤로 백이 한 턴을 더 두고, 백의 두 번째 차례(T2)가 시작될 때**
발동한다(`fireAt = ply + 4`). `augments.js` 는 프롬프트가 언제 뜨든 가짜 api 가 받아 줘서 시점을 못 잡아냈다.
`feedback.js` 는 그 수를 정확히 세서 두기 때문에 시점이 어긋나면 창이 안 떠서 바로 걸린다.

**전부 통과 (48항목).** 이제 피드백 10건은 모두 화면에서 확인된 상태다.

### 15:45 — K1b 는 '그 칸의 3개 중 2개' 다

'첫 킹 증강 개수 오류 (1개 더 있음)' 의 정체였다. `K1b` 는 **다음 드래프트에서 그 칸의 선택지 3개 중 2개**를 고르는 증강인데,
구현은 `'선택지 2개' 가 아니라 '드래프트를 한 번 더'` 로 처리하면서 **회차마다 칸을 다시 골랐다**(`chooseCell`).

보통은 같은 칸이 다시 나와서 결과가 같다 — 방금 고른 카드가 '이미 보유한 증강입니다' 로 잠겨 오니 3개 중 2개가 된다.
어긋나는 건 **그 칸의 남은 둘이 다 잠겼을 때**다. 그러면 '칸 전체가 잠기면 다른 기물 칸으로 넘어간다' 규칙에 걸려
**엉뚱한 기물의 증강**이 하나 더 들어왔다. 예를 들어 흑 퀸이 없는데 퀸으로 처치하면 퀸 1개 칸에서 고를 수 있는 건 `Q1a` 하나뿐인데,
두 번째 회차가 폰 칸으로 넘어가 폰 증강을 하나 더 줬다.

- `runDrafts` — 칸은 **첫 회차에만** 고르고, 두 번째는 같은 칸을 다시 펼친다.
  남은 둘이 다 잠겨 있으면 두 번째는 없다(진행 기록에 한 줄 남긴다). 다른 기물 칸으로 넘어가지 않는다.
- 드래프트 창 — `2번 중 1번째` → **`K1b · 이 칸에서 2개 · 1번째`**, 안내문도
  "이 칸의 선택지 중 2개를 고릅니다 — 지금은 n번째" 로. '다른 칸이 한 번 더' 로 읽히지 않게 칸 기준으로 적었다.
- 전역 룰 '드래프트 방식' 문구 — "K1b 를 갖고 있으면 드래프트를 한 번 더 돌린다" →
  "같은 칸을 한 번 더 펼쳐 그 3개 중 2개를 고른다(다른 기물 칸으로 넘어가지 않는다)".
  원본은 `tools/augment_data.py` 라 고치고 `python tools/build_js.py` 로 `data.js` 를 다시 만들었다.
- `K1b` 획득 안내도 "다음 드래프트에서 그 칸의 선택지 중 2개를 고릅니다" 로.

증강 감사에 시나리오 둘을 더했다 (75 → 77). 나이트로 처치해 **둘 다 N1\* 이 들어오는지**,
그리고 흑 퀸이 없는 판에서 퀸으로 처치했을 때 **`Q1a` 하나만** 들어오는지.
옛 로직으로 되돌려 보면 두 번째가 실제로 깨진다 (폰 증강이 딸려 들어온다).

### 13:52 — 규칙 · 용어: 바로가기 한 줄 · 전역 룰은 이름만

네 덩이(증강을 얻는 흐름 · 조작 · 전역 룰 · 용어)가 세로로 이어져 있어 용어 하나 보려고 한참 굴려야 했다.
도감과 같은 자리·같은 모양으로 **바로가기 한 줄**을 붙여 바로 건너뛴다.

전역 룰은 본문이 증강 ID(R6a · N3b …)가 섞인 예외 설명이라 처음 보는 사람에게는 읽을거리가 아니다.
**이름만 먼저 보여 주고 본문은 접어 뒀다** — 궁금한 줄만 눌러서 편다(`<details>`).
설계 사유(`why`)는 09-09 부터 화면에 안 그린다. 이제 전역 룰 여섯 줄 + 용어 일곱 장이 한 화면에 들어온다.

### 13:48 — 증강 도감: 카드를 내용만큼만 높이기

티어 칼럼을 subgrid 로 묶으면서 행을 `1fr` 로 뒀더니 한 기물 12장이 **가장 긴 카드 높이로 다 같이 늘어나** 있었다.
짧은 카드는 절반이 빈칸이라 한 화면에 여섯 장밖에 안 보였다. 행을 `auto` 로 바꿔 **같은 줄끼리만** 높이를 맞춘다
— 표가 어긋나 보이는 일은 그대로 없다. 카드 안쪽 여백과 줄간격도 한 단계씩 좁혔다.
1440×900 에서 본문 높이 582px → 541px (보이는 높이 519px), 이제 한 기물 12장이 거의 한 화면이다.

### 13:44 — '판을 보는 중입니다' 를 크게

증강 고르는 중에 판을 보면 뜨는 바가 16px / 14px 이라 판에 묻혔다. 아직 안 골랐다는 걸 놓치면 30초가 그냥 간다.
제목 22px · 부제 15px · '증강 고르기로 →' 18px 로 키우고 여백·모서리도 한 단계 키웠다.

### 13:41 — 포영 · 지정불가 회귀 테스트 18항목 · B3c 잠긴 선택지

"포영/지정불가 작동 안 함" 을 확인하려고 증강 하나가 아니라 **엔진 쪽 규칙을 통째로** 잠갔다 (`node tools/test/engine.js`).

- 지정불가 — 처치 대상에서 빠지고, 자기도 못 움직이고(`restrictedBy` 가 '지정불가' 를 돌려준다),
  한 ply 로는 안 풀리고 정해진 ply 에 정확히 풀린다. 킹은 애초에 대상이 아니다.
- 포영 — 칸을 비우고 나갔다가 제자리로 돌아온다. 그 사이 그 칸은 지나갈 수 있고,
  돌아올 때 차지하고 있던 기물은 피아 상관없이 제거돼 무덤으로 간다. 킹과 R3c 룩은 나가지 않는다.

**28항목 전부 통과 — 지금 코드에서는 둘 다 제대로 돈다.**

증강 감사(`tools/test/augments.js`)에는 B3c 시나리오를 하나 더 넣었다. 스폰 칸이 막힌 종류가
고른 뒤 '불가' 가 아니라 **처음부터 잠겨서** 오는지 본다 — b1 · g1 을 나이트가 막으면 나이트는 잠기고 룩만 고를 수 있다.
잠긴 선택지를 보려고 하네스의 `pickOption` 이 받은 선택지를 `T.opts` 에 남기게 했다. 75항목 전부 통과.

### 13:39 — 피드백 10건 전수 확인

`피드백.md` 는 원문 보존용이라 처리된 항목도 남아 있다. 지금 빌드로 하나씩 다시 확인한 결과다.

| 피드백 | 지금 |
|---|---|
| 시작화면에 도감 · 규칙이 없다 | **이미 있음** — 메인 화면 아래 줄 `증강 도감` · `규칙 · 용어` (눌러서 확인) |
| 도감 가독성 (기물별/티어별) | **이미 있음** (기물 · 처치 수 차림표) + 위 13:48 |
| 첫 킹 증강 개수 오류 | 도감·데이터는 정상(티어당 3개). 원인은 `K1b` 였다 → 위 15:45 |
| 증강 색으로 지속 유형 구분 | **이미 있음** — 턴제한 파선 · 횟수제한 점선 · 영구지속 실선 + 칩 + 범례 |
| '판을 보는 중입니다' 크게 | 위 13:44 |
| 규칙/용어 UI | 위 13:52 |
| 전역 룰 밑 설명 빼기 | 설계 사유는 09-09 부터 안 그림. 남은 본문도 위 13:52 에서 접었다 |
| 드래프트 방식 설명 | **이미 고쳐짐** (09-14 13:14) — "그 칸의 선택지 3개를 그대로 펼친다" |
| 포영 / 지정불가 작동 안 함 | **재현 안 됨** — 위 13:41 (28항목) · 증강 감사 75항목도 전부 통과 |
| 폰 두 칸 전진 칸 수 | **이미 고쳐짐** — 기본 배치 1~3칸 · 이미 움직인 폰 1~2칸 (09-17 13:10) |
| 비숍 '비밀'(B3c) | **이미 고쳐짐** — 그 처치에 바로 걸리고(regrant) · 2턴 생존 · 막힌 칸은 잠겨서 온다 |

**'첫 킹 증강 개수 오류 (1개 더 있음)' 은 도감이 아니라 `K1b` 였다.**
증강표(`tools/augment_data.py`) · `data.js` · 도감 화면 셋 다 킹은 1 · 3 · 6 · 11 티어마다 3개씩, 합쳐서 12개로 정상이다
(K1a K1b K1c / K3a K3b K3c / K6a K6b K6c / K11a K11b K11c). 증강이 하나 더 들어오던 건 `K1b` 의 두 번째 회차가
다른 기물 칸으로 넘어가던 것이었다 — 위 15:45 에서 고쳤다.

### 13:10 — 엔진 회귀 테스트 (브라우저 없이) · P1b 폰 전진 거리 잠그기

피드백 '플레이 2' (두 칸 전진 증강을 이미 움직인 폰이 먹어도 3칸을 간다) 를 다시 보라는 이야기가 있어 확인했더니 **이미 고쳐져 있었다** — `9db7c07` 에서
`far = moved ? 2 : 3` 으로 바뀌었고 지금 HEAD 에도 그대로 있다 (`game/src/engine.js:360`). `피드백.md` 는 원문 보존용이라 처리된 항목도 남아 있다.

대신 이 동작을 잡아 두는 것이 없어서 테스트를 새로 넣었다. 지금까지 테스트는 `online.js`·`augments.js` 둘 다 중계 서버와 헤드리스 크로미움이 있어야 돌아서,
규칙 한 줄을 확인하려 해도 판을 다 띄워야 했다. `engine.js` 는 DOM 을 안 쓰는 순수 규칙 코드이므로 `vm` 에 가짜 `window` 만 쥐여 주면 node 에서 바로 불린다.

`tools/test/engine.js` (10항목) — 기본 배치 폰 1~3칸 · 이미 움직인 폰 1~2칸 · 증강이 없을 때의 1~2칸과 1칸 · 지나갈 칸이 막힐 때 세 가지 ·
흑 진행 방향 · `p1b` 표시가 3칸 전진에만 붙는지(원래 규칙인 2칸 전진이 증강 횟수를 깎으면 안 된다) · p1b 로 승격 칸에 닿을 때 승격 네 가지.

옛 로직(`far = 3` 고정)으로 되돌려 보니 **2항목이 실제로 깨진다** — 이미 움직인 폰의 전진이 `e3,e4` 가 아니라 `e3,e5` 로 나오고(2칸이 아예 사라진다),
e6 폰이 승격 칸에 닿지 못한다. 잡아야 할 것을 잡는다는 뜻이다. 코드는 안 건드렸다.

---

## 2026-09-16

### 10:54 — README 에 오늘 고친 것 마저 적기

대국 시계·수 예약 절만 있었다. '무슨 일이 일어났는지 보여주기' 에 상대 화면 효과·B3a 카드·알림 구분을, '온라인 대전' 에 온라인에서만 어긋나던 것 표를,
'폴더 구성' 에 `tools/test/`, '검증' 에 두 테스트와 결함 8~11 을 더했다. 코드는 안 건드렸다.

### 10:18 — 증강 72종 발동 감사 · 브라우저 테스트를 저장소에

"증강 중에 발동 안 되는 거 다 고쳐 줘" 를 스크린샷에 보인 것만으로 끝내지 않으려고, 72종 전부를 자동으로 발동시켜 봤다.
증강마다 조건이 성립하는 판을 FEN 으로 만들고, 얻거나(onGain) · 처치하거나 · 두거나 · 턴이 시작되거나 · 체크가 걸리게 한 뒤
판·효과·플래그가 실제로 바뀌는지 본다(`tools/test/augments.js`, 74 시나리오). **전부 통과** — 로컬 흐름에서는 발동하지 않는 증강이 없다.
처음 실패한 6개는 내가 만든 판이 잘못된 것(시작부터 체크 상태, 룩을 대각선으로, 효과가 만료된 뒤 검사)이었다.

온라인 테스트(`tools/test/online.js`)에는 턴 시작 예약 증강(B3a) 이 **판을 받은 쪽 화면에서 프롬프트를 띄우고, 소환된 폰이 상대 화면에도 오는지**를 더했다.
그 김에 내 비밀 증강이 발동할 때 "내 비밀 증강이 공개되었습니다" 카드가 발동 카드와 연달아 뜨던 걸 뺐다 — 공개는 상대에게만 새 소식이다.

테스트는 `tools/test/` 에 넣었다 (`README.md` 에 돌리는 법). 중계 서버를 띄우고 `node online.js` · `node augments.js`.

### 01:15 — 수 예약 · 상대 증강 선택 표시 · 온라인에서 어긋나던 것들

온라인 대전 스크린샷으로 받은 지적을 한꺼번에 처리했다. 두 브라우저를 한 방에 앉혀 돌리는 테스트로 고치기 전 상태를 재현하고, 고친 뒤 전부 통과시켰다.

**수 예약 (premove)** — 상대 차례에 내 기물을 끌어 놓아 두면 내 차례가 오는 순간 그 수가 나간다.
`ui.js` 에 `premove`·`pmFrom`·`pmDests` 를 두고, 둘 수 없는 상황(`canControl` 거짓)에서도 내 기물이면 보라색으로 골라 놓을 수 있게 했다.
내 차례가 열리면(`onUpdate` → `schedulePremove`, 0.18초 뒤) 그때 판에서 합법인지 다시 검사하고, 아니면 토스트로 알리고 접는다. 승격이면 퀸.
AI 대전·온라인에서만 뜻이 있다. 턴 바에 예약 칩(누르면 취소), 판 우클릭·Esc 로도 취소.

**상대가 증강을 고르는 중** — 상대 화면의 30초 타이머와 같은 값을 이쪽에도 띄운다.
드래프트 쪽이 `note('draft', {left, limit, piece, tier})` 를 0.5초마다, 끝나면 `draftEnd` 를 보낸다(`relay.py` 는 note 를 그대로 흘려보내므로 서버는 안 고쳤다).
받는 쪽은 판 가운데 `#oppdraft` 카드(클릭을 막지 않는다 — 그동안 판을 보거나 수를 예약할 수 있다)와 오른쪽 `#oppdrafttimer` 막대에 남은 초를 그린다.
턴 바의 '상대가 증강을 고르는 중' 도 이제 이 신호로만 뜬다. 예전에는 시계가 멈추기만 하면 그렇게 적어서, 상대가 **도감을 열어도** 그 말이 떴다.

**시계** — 도감·규칙·계정·재대국 창은 `overlay(…, {keepClock: true})` 로 시계를 멈추지 않는다. 상대 차례에 도감을 열어 두면 상대 시간이 공짜로 서던 것.
증강 선택은 창이 뜨기 전 '판을 보여 주는 0.45초' 부터 멈춘다. 시계를 멈추는 창의 수를 `pauseDepth` 로 따로 세서, 창이 겹쳐도 마지막 하나가 닫힐 때만 다시 간다.

**"비밀 증강이 공개되었습니다"** — 비밀도 아닌 첫 증강이 상대 화면에 그렇게 떴다.
`revealed` 에는 비밀이 아닌 증강도 얻는 순간 들어가는데(`grantAug`), `adoptRemote` 가 새로 들어온 id 를 전부 '공개' 로 알렸다.
비밀이 아니면 '상대의 증강 획득' 으로, 비밀이면 '공개' 로 나눴다. 상대가 비밀 증강을 **얻은 사실**(가면 `?w1`)도 이제 알린다.

**상대 차례에 상대 증강 버튼이 뜨던 것** — `renderActions` 가 `isHuman(turn)` 만 봐서 온라인에서는 상대 차례에 상대의 횟수제한 증강(N3a 등)이 내 화면에 버튼으로 떴다.
눌러도 내 판만 바뀌고 곧 덮어써져 '발동이 안 되는' 것처럼 보였다. `myTurn()` 으로 막고, `Game.activate` 도 온라인에서는 내 차례·내 증강만 받는다.
발동한 뒤에는 바로 `Net.push` — 예전에는 다음 수를 둘 때까지 상대 화면이 옛 판이었다. K11a·N3a 는 2인 대전에서 UI 로 발동해 정상임을 확인했다.

**B1b 가 온라인에서 한 번도 발동하지 않던 것** — `onOppMoved` 를 '둔 쪽' 화면에서 돌렸는데, 그 화면에는 상대의 비밀 증강이 가면으로만 있어 구현이 비어 있다.
온라인에서는 판을 받은 쪽(`adoptRemote`)이 자기 증강으로 돌린다. 2인·AI 는 그대로.

**상대 증강이 내 기물을 지울 때** — 이쪽 화면에서는 기물이 소리 없이 사라졌다. `adoptRemote` 가 받기 전후 판을 비교해 '방금 둔 수' 가 아닌데 바뀐 칸을 빛내고(`remoteFx`),
기록의 `⚡ [id] … 발동` 줄을 상대 화면에만 떴던 배너로 보여 준다.

**B3a·B3b (점수 합 홀수/짝수면 폰 소환)** — 카드에 **지금 아군 점수 합과 홀짝, 조건 충족 여부**를 적는다. 판정은 한 번뿐이라 결과를 `flags[id+'Done']` 에 남기고,
판정 순간에는 화면 가운데에 '발동 — 폰 소환' 또는 '불발' 카드를 띄운다(`api.event`). 예전에는 불발이면 토스트 한 줄뿐이라 증강이 그냥 사라진 것처럼 보였다.

**기본 제한시간** — 10분+5초 → **5분+3초** (메인·온라인 선택 상자와 `Game.timeControl` 모두). 증분은 한 수 둘 때마다 그쪽 시계에 더해지는 초로, README 와 규칙 창에 적었다.

`index.html` 에 파비콘 자리(`data:`)를 넣어 콘솔 404 를 없앴다.

---

## 2026-09-14

### 16:31 — 포크로 옮기기

계정 작업 4개 커밋을 포크(`Argenviol/20260914_1608`)의 `김준영_20260914_1608` 브랜치에 올렸다.

포크의 `main` 은 원본의 `김준영_20260910_1740` 병합 지점이라 **09-12 브랜치가 빠져 있었다.**
그래서 4개 커밋이 건드리는 파일만 09-12 상태로 먼저 맞추고(`chore:` 커밋), 그 위에 4개를 그대로 얹었다.
09-12 가 건드린 나머지(`augments.js` · `game.js` · `ai.js` · `tools` · `기획검토.md`)는 계정 작업과 겹치지 않아 옮기지 않았다.
원본으로 옮길 때 09-12 브랜치 위에 얹으면 그 `chore:` 커밋은 빈 커밋이 된다.

옮긴 뒤 확인한 것 — `relay.py` 를 띄워 정적 파일과 `/api/auth/*` 응답, 헤드리스 브라우저로 메인 화면·로그인 창·가입 시도.
수퍼베이스가 없는 상태라 계정 API 는 503 과 함께 "아직 연결되지 않았습니다" 만 돌려주고, 게임과 브라우저 전적은 그대로 돈다.
자바스크립트 오류나 404 는 없었다. `bump.py` 로 캐시 스탬프를 올렸다.

### 16:08 — 새 브랜치 `김준영_20260914_1608`

계정은 새 기능이라 규칙대로 새 브랜치로 뺐다. 이 세션은 원본 저장소에 쓰기 권한이 없어(앱 미설치),
사용자가 만든 포크에 올리고 사용자가 원본으로 옮긴다.

### 15:32 — '통계 켬/끔' → '전적 저장 켬/끔'

통계 스위치는 켜고 꺼도 화면에서 달라지는 게 없어 무슨 스위치인지 알 수 없었다.
눈에 보이는 전적으로 이름을 바꾸고, 끄면 **전적도 안 남게** 했다 — 브라우저 기록, 계정, 익명 통계 모두.
저장 키(`mujeChess.stats`)는 그대로라 예전에 꺼 둔 사람은 꺼진 채로 이어진다.

### 15:27 — 로그인하면 전적이 이어진다

전적은 브라우저(localStorage)에만 남아서 기기를 바꾸면 사라졌다. 아이디·비밀번호로 가입하면 계정에 합쳐진다.

**경로는 통계와 같다** — 브라우저 → 렌더(`relay.py`) → 수퍼베이스. 키와 비밀번호 검사는 서버에서만 한다.
파이썬 표준 라이브러리만 썼다(PBKDF2 는 `hashlib`, 토큰은 `secrets`). 표 셋: `accounts` · `account_sessions` · `account_records`.

- 비밀번호는 PBKDF2-SHA256 20만 회 해시로만, 세션 토큰은 SHA-256 해시로만 저장한다. 토큰은 90일.
- 로그인·가입은 IP 당 5분에 12회까지. 아이디는 2~16자(한글·영문·숫자·_), 대소문자 구분 없이 하나.
- **합치기** — 판마다 id 를 붙이고 id 기준으로 합친다. 예전 기록에는 id 가 없어서 시각+모드로 만들어 준다.
  로그인할 때 브라우저 전적을 같이 보내 계정 것과 합치고, 그 뒤로는 판이 끝날 때마다 합친다. 최신순 100판.
- '기록 지우기' 는 로그인 중이면 계정 쪽도 지운다.
- 서버에 수퍼베이스가 없으면(로컬 실행) 로그인 창이 "아직 연결되지 않았습니다" 만 알린다. 게임은 그대로.
- `file://` 로 열면 서버가 없으니 계정 칸을 아예 안 그린다.

가짜 수퍼베이스를 띄워 API 를 전부 돌렸고(가입·중복·틀린 비번·합치기·로그아웃·지우기), 헤드리스 브라우저로
가입 → 새로고침 → 다른 기기 로그인 → 같은 전적이 보이는 것까지 확인했다.

**아직 안 된 것** — 렌더에 수퍼베이스 주소·키가 안 들어가 있다(어느 프로젝트에도 `plays` 표가 없어서 익명 통계도 지금은 버려지고 있다).
표 만드는 SQL 은 `docs/분석_수퍼베이스_설정.md` 끝에 붙였다. 프로젝트를 정하고 환경변수를 넣으면 그때부터 동작한다.

### 15:15 — 답변 끝에 배포 주소

바로 열어 볼 수 있게, 답변은 항상 https://unnamed-chess.onrender.com/ 으로 끝내기로 했다. `CLAUDE.md` 에 적었다.

### 13:14 — 주석·문서에 남은 옛 숫자 전수 확인

소스 주석에서 숫자가 들어간 줄을 모아 코드 값과 대조했다. 대부분은 맞았고(64칸, 30초, 10분, 티어 4칸, 개정 35건 …),
뒤처진 것은 드래프트 설명 두 곳뿐이었다. 둘 다 처음 잡았던 **"티어의 18개 중 무작위 3개"** 방식이 그대로 남아 있었다.
지금은 처치한 기물의 칸(선택지 3개)을 그대로 펼친다.

- `tools/build_xlsx.py` — 증강표 엑셀 첫 줄 안내문. 고치고 `무제체스_증강표_v2.xlsx` 를 다시 만들었다.
- `docs/기획검토.md` 4절 — 확정한 방식으로 고치고, 처음 안에서 왜 바꿨는지 한 줄 남겼다. 5절 3번의 "무작위 3장" 표현도 맞췄다.

### 13:11 — ai.js 머리말의 "정석 오프닝 15종"

오프닝 북은 46줄인데 파일 머리말은 처음 만들 때 숫자(15종) 그대로였다. 46줄로 고쳤다.
README 는 13:06 에 이미 맞춰 두었다.

### 13:08 — "73종" 이라고 적혀 있던 곳 셋

증강은 72종인데 `augments.js` 머리말과 `tools/build_impl_xlsx.py` 의 설명문 두 곳이 "73종" 이었다.
킹 1개 티어에 `K1d` 를 넣었다 뺐을 때 숫자만 남은 것이다. 셋 다 72로 고치고 구현표 엑셀을 다시 만들었다
(훅 57종 + 패시브 15종 = 72종). `bump.py` 로 캐시 스탬프도 올렸다.

### 13:06 — README 가 코드보다 뒤처져 있던 것

기록에는 고쳤다고 적혀 있는데 README 에는 예전 그대로 남아 있던 곳들을 코드 기준으로 맞췄다.

| 곳 | 예전 | 지금 |
|---|---|---|
| 게임 모드 | AI · 2인만 | 온라인 대전 추가 |
| 환경설정 | "항복은 없습니다" | 항복 있음, 중단은 뺐다고 적음 |
| 이모티콘 | "구현하지 않았습니다" | 온라인 대화 탭의 이모티콘 10개 |
| 대국 시계 | — | 증강 고르기 30초 제한 추가 |
| 오프닝 북 | 25줄 | 46줄 (드문 첫 수 포함) |
| AI 난이도 표 | 0.2 / 0.9 / 2.6초 · 실수 14% | 0.32 / 1.5 / 3.6초 · 실수 8% (상향 뒤 값) |
| 폴더 구성 | `stats.js` · `server/` · `render.yaml` 등 빠짐 | 실제 파일 전부 |

코드는 안 건드렸다.

### 10:17 — 작업 기록에 시간을 붙이기로

이 파일은 날짜 단위로만 나뉘어 있어서, 하루에 여러 번 손댄 것이 어느 순서였는지 알기 어려웠다.
이제 날짜 아래에 `### HH:MM — 제목` 으로 시간을 붙여 적는다 (한국 시간).
같은 규칙을 `CLAUDE.md` 에 적어 두어, 다음 세션에서도 작업할 때마다 여기에 기록하고 **지정된 브랜치에만** 올리게 했다.
브랜치 표에 지금 브랜치(`김준영_20260912_1924`)도 넣었다.

---

## 2026-09-12

### 마지막 수는 '가장 마지막에 둔 쪽' 기준으로

노란 칠을 `Game.lastMove` 에서 뽑고 있었는데, 이 값은 **내가 둘 때만** 갱신된다.
온라인에서 상대 판을 받아 오면(adopt) 갱신되지 않아 **내 옛 수가 계속 노랗게 남았다.**
판 안의 `lastBySide` 를 쓰도록 바꿨다 — 상태 전송에 같이 실려 오므로 양쪽 화면이 같아진다.

### 잡은 걸 먼저 보여 주고 증강 창을 연다

처치한 순간 바로 덮개가 떠서, 무엇을 어떻게 잡았는지 못 보고 카드부터 봤다.
판을 한 번 그려 주고 0.45초 뒤에 창을 연다. 그동안 턴 바에 **"백 — 증강을 고르는 중입니다"** 가 뜬다.
'판 보기'로 덮개를 치워도 이 표시는 남아서, 지금 무엇을 기다리는 중인지 알 수 있다.

**턴이 시작될 때 일어난 처치도 바로 증강으로 이어진다.** 예전에는 드래프트가 `doMove` 안에서만
돌아서, `N6b`(멀리 있는 상대 나이트 처치)처럼 턴 시작에 잡는 증강은 창이 **다음에 수를 둘 때**
뒤늦게 떴다. 잡은 것과 증강이 한 박자 어긋나 보이던 원인이다.

### N3b — 양쪽 한 턴씩

문구는 "다음 2턴 동안" 이었는데 실제로는 **상대 2턴 + 내 1턴** 이라 양쪽이 안 맞았다.
`untilEachTurns()` 를 만들어 **양쪽이 정확히 한 턴씩** 쓰게 하고 문구도 그렇게 고쳤다.

### 증강 카드가 너무 길던 것

카드마다 본문 아래에 `#용어` 태그 줄이 또 붙어 있었다. 본문에 이미 같은 말이 색칠돼 있고
눌러서 뜻도 볼 수 있으니, 오른쪽 증강 탭에서는 태그 줄을 뺐다. 카드 높이 120 → **88px**.
(증강을 고르는 화면에는 그대로 둔다 — 거기서는 훑어보기에 도움이 된다.)

### 비밀이 아닌데 '비밀' 로 뜨던 것

가면 해독에 실패한 증강을 `비밀 · 알 수 없는 증강` 으로 그리고 있었다.
비밀 증강이 아니라 **그리지 못한** 것이라, 없는 정보를 지어내는 셈이었다.
`표시할 수 없는 증강 (새로고침하면 돌아옵니다)` 으로 바꿨다.

### 대화 스크롤바

도감과 같은 얇은 막대(8px)로 맞추고 말풍선과 띄웠다.

---

## 2026-09-11

### 마지막 수를 노란색으로

어떤 기물이 어디서 어디로 갔는지 판만 보고 알 수 있게, 출발 칸과 도착 칸을 노랗게 칠한다.
예전에는 회색(`fill-strong`)이라 판 색과 거의 구분이 안 됐다.

노랑을 고른 이유는 남은 자리가 거기뿐이라서다 —
빨강은 처치·체크, 초록은 대상 지정, 주황은 증강 발동, 파랑은 선택·이동, 보라는 살펴보기에 이미 쓰고 있다.
그 칸을 선택하거나 이동 대상이 되면 파랑이 이긴다. 지금 할 수 있는 일이 먼저다.

### 판 크기가 계속 흔들리던 것

판 아래에 **포영 칩(`#phased`)과 발동 버튼(`#actions`)** 이 있었다.
둘 다 상황에 따라 나타났다 사라지는데, 그때마다 그 높이만큼 판이 줄었다 늘었다 했다.
진영 바도 마찬가지였다 — 잡은 기물이 쌓이거나 이름이 길어지면 줄이 늘어 바가 높아졌다.

판 칼럼을 **높이가 변하지 않는 것들만** 남겼다.

| 예전 | 지금 |
|---|---|
| 턴바 · 상대바 · 판 · 내바 · 포영칩 · 발동버튼 | 턴바 · 상대바 · 판 · 내바 |

- 포영 칩과 발동 버튼은 오른쪽 탭 위로 옮겼다 (비어 있으면 자리도 안 차지한다)
- 진영 바를 한 줄로 고정했다. 폭이 모자라면 줄이는 쪽으로만 간다
- 잡은 기물 줄도 높이를 18px 로 못 박았다. 비었을 때와 찼을 때가 1px 달라도 판이 줄어든다
  (우세 숫자를 맨 앞으로 옮겨, 기물이 넘쳐 잘려도 숫자는 남게 했다)

1280×720 에서 착수 · 처치 · 드래프트 · 포영 발생 · 포영 복귀 · 잡은 기물 15개 · 발동 버튼까지
전 구간 **52px 고정**, 1024×640 에서 42px 고정. 겹침 0, 페이지 스크롤 없음.

### 포영이 한 턴 늦게 걸리던 것 (Q3b)

포영 증강 6종을 다 따라가 봤더니 **`Q3b` 하나만 방식이 달랐다.**
얻는 즉시 거는 게 아니라 '충전' 을 주고 **다음 내 차례에 눌러서** 쓰게 돼 있었다.
그 사이에 상대 턴이 한 번 끼니까, 정작 지키려던 퀸이 먼저 잡혔다. 포영이 안 걸리는 것처럼 보인 이유다.

다른 포영 증강과 똑같이 **증강을 고른 그 턴에 바로** 걸리게 했다.
퀸은 고른 순간 판에서 빠지고, 상대 턴을 통째로 건너뛴 뒤 내 차례가 시작될 때 돌아온다.

| 시점 | 턴 | d1 |
|---|---|---|
| 증강 획득 직후 | 나 | 비어 있음 |
| 내 수 끝 (상대 차례) | 상대 | 비어 있음 |
| 상대 수 끝 (내 차례) | 나 | 퀸 복귀 |

나머지 다섯(`P1c`·`R3b`·`N6a`·`Q6a`·`Q11b`)은 이미 즉시 발동이라 그대로 뒀다.

### 증강 문구 다듬기 (65개)

`N1b` 를 보면 **"적을 처치한 나이트와 자신의 비숍 중 하나의 위치를 교환합니다"** 였다.
'자신의' 가 나이트를 가리키는지 나를 가리키는지 알 수가 없고, 언제 일어나는 일인지도 안 적혀 있었다.
→ **"나이트가 적을 처치할 때마다, 그 나이트와 아군 비숍 하나의 위치를 교환합니다."**

뜻은 그대로 두고 말만 고쳤다. 기준은 다섯 가지다.

- **누구 것인지 흐린 말** — '자신의' → '아군 / 상대'
- **번역투** — '~할 시', '~하는 경우', '~한 상태라면' → 평이한 말
- **조건이 뒤에 붙어 주어가 헷갈리던 문장** — '무엇을 하면 → 무엇이 된다' 순서로
  (`P1c`·`R1c`·`R3b`·`P6c`·`B3c` 등)
- **용어 통일** — 문구는 '강화', 화면은 '증강' 으로 갈려 있었다. 화면 표기인 **증강**으로 맞췄다 (10곳)
- **표현** — '기물점수' → '점수', '가로열' → '가로줄', '스폰 위치' → '시작 칸'

뜻이 흐려서 구현을 열어 보고 고친 것들:

| | 예전 | 지금 |
|---|---|---|
| `P6c` | "전방 2칸 이내에 적 기물이 있을 시 교환합니다" (무엇과?) | "그 폰 앞 2칸 이내에 있는 적 기물 하나와 **위치를** 교환합니다" |
| `B3c` | "스폰 위치에 소환합니다" | "잃었던 아군 기물 하나를 **그 기물의 시작 칸**에 되살립니다" |
| `K3b` | "점수 차이만큼, 원하는 기물을 배치" (무엇이 차이만큼?) | "점수 차이만큼 **예산을 받아**, 그만큼 … (폰 1 · 나이트 3 …)" |
| `B1b` | "비숍이 서 있는 발판과 같은 색의 칸" | "비숍에 인접한 칸 중 **비숍이 선 칸과 같은 색인 칸**" |

데이터는 `tools/augment_data.py` 한 곳에서 고치고 엑셀·`data.js` 를 재생성했다.

### AI 난이도 상향

| | 예전 | 지금 |
|---|---|---|
| 초급 | 3수 · 200ms · 흔들림 55 · 실수 14% | 4수 · 320ms · 흔들림 38 · 실수 8% |
| 중급 | 6수 · 900ms · 흔들림 14 · 실수 2% | 8수 · 1500ms · 흔들림 8 · 실수 0.4% |
| 고급 | 20수 · 2600ms · 흔들림 0 | 26수 · 3600ms · 흔들림 0 |

'흔들림'은 뿌리에서 최선수와 이만큼 차이 나는 수까지 허용하는 폭이고, '실수'는 아예 아무 수나 두는 확률이다.

같은 국면에서 검토하는 수가 늘었다 — 중급 79k → 321k 노드(깊이 6 → 7), 초급 17k → 21k(3 → 4수).
새 중급 vs 예전 중급을 붙여 **15판에서 10승 2패 3무(승점률 77%)**.
(판수를 벌려고 생각시간은 양쪽 다 5분의 1로 줄여 붙였다. 15판이면 방향은 보이지만
 몇 %인지까지 못 박을 표본은 아니다.)

### N1c — 다음 1회로, 연 처치에는 발동 안 함

`N1c`(적을 처치한 나이트 주변 한 칸 광역 제거)가 **영구지속**이었다.
1처치짜리 첫 티어에서 매번 광역 제거가 나오는 건 너무 셌다.

- **횟수제한 1회**로 바꿨다
- **이 증강을 열어 준 처치에는 발동하지 않는다.** 다음 나이트 처치부터다.
  (증강 훅을 다시 흘려보낼 때 `regrant` 표시를 달아, 그런 증강이 스스로 걸러내게 했다 —
  `P1c`·`B3c` 처럼 연 처치에 발동해야 하는 증강은 그대로 둔다)
- 주변이 비어 있었으면 쓴 것으로 치지 않는다. 허공에 날리면 억울하다.

### 기록을 누를 때마다 판이 출렁이던 것

되감기 바가 턴 바 자리를 대신 쓰는데 **두 바의 높이가 달랐다.** 되감기 바는 줄바꿈이 열려 있어
버튼 넷이 두 줄이 됐고(44px → 58px), 그 차이만큼 판이 줄었다 늘었다 했다.
되감기 바를 한 줄로 줄이고 두 바의 높이를 44px 로 묶었다. 이제 어느 줄을 눌러도 칸 크기가 그대로다.

### 되감기에서 나오는 길

'현재로 돌아가기' 가 판 위 바 끝에만 있어서, 기록을 누르던 손을 멀리 옮겨야 했다. 세 가지를 더 뒀다.

- **기록 패널 맨 위의 띠** — 누르던 그 자리에서 바로 나온다
- **같은 줄을 다시 누르면** 현재로 돌아온다
- **Esc**

바 안의 버튼도 `← / → / 현재로 ✕` 로 줄여 한 줄에 들어간다.

### 기록 아무 줄이나 눌러 그때 판 보기

되감기 자체는 있었는데 **'→' 가 들어간 착수 줄에만** 스냅샷이 붙어 있었다.
정작 그때 판이 궁금한 건 처치·증강 획득·⚡발동 줄인데 그건 눌러도 아무 일이 없었다.
이제 한 수 동안 쌓인 줄을 전부 그 수의 판에 묶는다.

**시작 국면도 한 장 남긴다.** 안 그러면 '게임 시작' 줄이 첫 수 뒤 판에 붙어 버린다.

### 내 차례 표시

턴 바 글자만으로는 판을 보다가 놓친다. 내가 둘 수 있는 상태면 **판 둘레가 은은하게 뛴다.**
상대 차례·되감기 중·증강 고르는 중에는 꺼진다. 움직임을 줄이는 설정이면 깜빡이지 않고 테두리만 남는다.

### 용어를 눌러 뜻 보기

증강을 고르는 화면에서 '포영' 을 눌러도 뜻이 안 나왔다. 용어가 **카드 버튼 안에** 들어 있어서
누르면 그 증강이 골라져 버리기 때문이다. 캡처 단계에서 먼저 잡아 클릭을 거기서 끊고 설명 풍선을 띄운다.
도감·증강 탭·발동 알림 등 용어가 나오는 곳이면 어디서나 눌린다.

### 교환은 누구와 됐는지 적는다

'R6c — 교환했습니다' 만으로는 무엇이 어디로 갔는지 알 수가 없었다.
기록에 `⇄ 백 룩 a1 ↔ 흑 폰 a7 위치 교환` 으로 남기고, **증강 카드에도 같은 내용**을 붙인다.
카드 메모는 온라인으로 안 보낸다 — 비밀 증강이면 메모가 곧 정체다. 상대는 기록 줄로 같은 내용을 본다.

### 중단을 없앴다

중단과 항복이 따로 있으니 뭐가 다른지 알기 어려웠다. **항복만 남겼다.**
중단은 '승패를 안 남기고 나가기' 였는데, 그건 온라인에서 질 것 같을 때 빠져나가는 데 쓸 수 있다.
그냥 나가고 싶으면 메인으로, 판을 새로 하려면 다시 시작이 이미 있다.

### 체크가 걸리면 판이 작아지던 것

턴 바가 줄바꿈되게 열려 있어서, **'체크!' 칩이 붙는 순간 두 줄이 되고** 그만큼 판이 줄었다
(44px → 74px, 칸 크기도 같이 줄었다). 체크가 풀리면 다시 커지니 화면이 출렁였다.
AI 대전은 '8수 앞 · 124k 검토' 칩까지 있어서 더 쉽게 넘쳤다.

턴 바를 **한 줄로 고정**하고 긴 글은 말줄임으로 접는다(원문은 툴팁). 칩은 어떤 폭에서도 안 줄인다 —
상태를 알려 주는 쪽이 설명글보다 중요하다.

### AI 오프닝이 늘 똑같던 것

정석 후보를 `sort(() => Math.random() - 0.5)` 로 섞고 있었다. **이건 고르게 안 섞인다.**
작은 배열에서 V8 은 삽입 정렬을 쓰는데 무작위 비교자를 주면 원래 앞에 있던 것이 앞에 남는다.
그래서 1.e4 다음에 1…e5 가 균등 14.3% 자리에 **25.3%** 로 나왔다. 피셔–예이츠로 바꿔 14.2% 가 됐다.

정석도 **26 → 46줄**로 넓혔다. 1.d4·1.c4·1.Nf3 은 책에 답수가 한두 개뿐이라
제대로 섞어도 매번 같은 수가 나왔다. 드문 첫 수(1.g3·1.f4·1.b3·1.Nc3)도 두 갈래씩 넣었다.

| 첫 수 | e4 | d4 | c4 | Nf3 | g3 | f4 | b3 | Nc3 |
|---|---|---|---|---|---|---|---|---|
| 답수 갈래 | 8 | 5 | 5 | 3 | 2 | 2 | 2 | 2 |

46줄 전부 실제로 둘 수 있는 수인지 엔진으로 전수 검사했다 (어긋난 줄 0).

### 마지막 수 · 증강을 판 옆으로

진영 바에서 **마지막 수 버튼이 처치 눈금을 덮고 있었다.** 눈금 '11' 의 숫자는 막대 오른쪽 끝을
넘어가는데, 바가 좁아지면 그 자리로 버튼이 밀려 들어왔다. 창 크기와 무관하게 겹쳤다 —
바 너비는 창이 아니라 **판** 에 딸려 있어서 창 기준 미디어쿼리로는 막을 수가 없었다.

**진영 바에는 처치 눈금만 남겼다.** 마지막 수와 증강 수는 오른쪽 탭 위로 옮겨
두 진영을 위아래로 나란히 놓는다. 눈금 막대에도 숫자가 나갈 만큼 오른쪽 여백을 줬다.

바가 짧아지면서 판도 커졌다 — 1280×720 에서 칸 43px → 52px.

### 도감 · 규칙 화면 — 스크롤바 두 개, 제각각인 카드

**스크롤바가 두 개 겹치던 것.** 본문 높이를 `68vh` 로 못 박아 뒀는데,
머리말과 닫기 버튼을 더하면 모달 높이(`90vh`)를 넘겼다. 그래서 모달도 같이 스크롤됐고,
내용이 카드 한가운데서 잘려 깨진 것처럼 보였다.
본문이 **남은 높이를 그대로 쓰게** 바꿔서 스크롤바가 하나로 끝난다.

**도감 카드 높이를 맞췄다.** 티어 칼럼마다 카드 높이가 제각각이라 표가 어긋나 보였다.
칼럼이 부모의 행을 그대로 쓰게 해서(subgrid) **한 기물 안의 카드는 모두 같은 높이**가 된다.
행 개수는 검색 결과에 따라 달라지므로 그릴 때 정한다.

**스크롤바가 카드에 붙어 있던 것.** 굵은 기본 스크롤바가 오른쪽 카드 테두리와 겹쳐 보였다.
얇게 바꾸고(8px, 배경 없는 트랙) 카드와 한 칸 띄웠다.

**규칙의 4단계 흐름이 3+1 로 쪼개지던 것.** 자동 배치라 폭에 따라 3개만 들어가면
네 번째가 혼자 다음 줄로 내려가 흐름으로 안 읽혔다. 4 → 2 → 1 로만 접게 했다.

---

## 2026-09-10

### 대국 통계 수집 (수퍼베이스)

증강별 선택률·승률, 평균 대국 시간, 어떤 기물을 많이 쓰는지를 보려면 **여러 사람이 둔 판**이 모여야 한다.
판이 끝날 때 요약을 한 번 보낸다.

**브라우저 → 렌더 서버 → 수퍼베이스** 순서로 간다. 브라우저에서 수퍼베이스로 바로 안 보낸다 —
정적 사이트라 브라우저에 넣은 키는 누구나 볼 수 있다. 서버를 거치면 키가 안 나가고,
서버가 **아는 칸만 골라 담아서**(service_role 키는 잠금을 통과하므로 필요하다) 넘긴다.
크기·횟수 제한도 서버에서 건다.

**단위는 '한 판' 이 아니라 '한 사람이 겪은 한 판'** 이다.
온라인에서는 상대의 비밀 증강이 내 화면에 가면으로만 오니 상대 줄을 대신 쓸 수 없다.
그래서 각자 자기 것만 보낸다 — 두 컴퓨터가 대국 id 를 맞출 필요도 없어졌다.

표는 둘. `plays`(승패·사유·수·시간·처치·보유 증강·기물별 이동/처치)와
`aug_picks`(제시된 3개·잠긴 것·고른 것·고민 시간·시간초과 여부). `play_id` 로 이어 증강별 승률을 낸다.

환경변수가 없으면 조용히 버린다. 통계가 안 되는 것 때문에 게임이 멈추면 안 된다.
중단한 판은 안 보낸다 — 승패가 아니라서 통계로 의미가 없다.

메인 화면 아래 **📊 통계** 버튼으로 끌 수 있다. 기기마다 만든 임의의 id 말고는 사람에 대한 걸 안 보낸다.

만드는 순서는 `docs/분석_수퍼베이스_설정.md` 에 적어 뒀다.

### 한 화면에 다 보이게 — 탭 한 줄 · 판 크기 실측

**대화 탭이 둘째 줄로 밀려나던 것.** 탭 칸을 `repeat(3, 1fr)` 로 못 박아 둬서
네 번째 탭이 갈 자리가 없었다. 칸을 개수에 맞춰 만들게 바꿔(`grid-auto-flow: column`)
숨은 탭은 자리를 안 차지한다. 이제 **대화는 진행 기록 바로 오른쪽**, 네 개가 한 줄.
안 읽음 표시도 글자를 바꾸는 대신 점으로 붙여, 숫자가 늘어도 탭 너비가 흔들리지 않는다.

**판 크기를 CSS 상수로 맞추던 것.** 화면을 줄이면 판이 아래로 넘쳤다.
`100vh - 268px` 처럼 '판 말고 나머지' 를 상수로 박아 뒀는데, 스트립은 잡은 기물이 쌓이면
줄이 늘고 좁아지면 접힌다 — 상수는 그때마다 틀린다.
한 번 그린 뒤 **실제 높이를 재서** 넘치는 만큼 칸을 줄이고, 수렴할 때까지 되풀이한다.
창 크기가 바뀌어도 다시 잰다.

**줄어들다 못해 쪼그라들던 것.** 스트립 너비가 판 너비에 묶여 있어서,
판을 줄이면 → 스트립이 좁아져 줄이 늘고 → 더 높아져서 → 판을 또 줄이는 악순환이 있었다.
스트립 너비에 420px 아래한계를 둬서 끊었다.

여섯 크기에서 넘침·겹침 없음을 확인 (칸 크기 px):

| 뷰포트 | 1600×1000 | 1440×900 | 1366×768 | 1280×720 | 1024×640 | 900×600 |
|---|---|---|---|---|---|---|
| 칸 | 82 | 74 | 49 | 43 | 33 | 36 |

### 온라인 대화 · 이모티콘

오른쪽 패널에 **대화 탭**을 붙였다(온라인일 때만 보인다). 판을 가리지 않는다.
다른 탭을 보고 있으면 `대화 2` 처럼 안 읽음 수가 뜬다.

이모티콘 10개를 한 번 눌러 바로 보낼 수 있게 뒀다 — 대국 중에 한글을 타자하는 건 부담이라,
빠른 반응은 이쪽으로 가게 했다.

### 증강 고르는 제한시간 30초

30초 안에 못 고르면 **고를 수 있는 것 중에서 무작위로** 하나 집는다.
여기서 그냥 멈추면 온라인에서는 상대가 영영 기다리게 된다.
10초가 남으면 숫자와 막대가 빨갛게 바뀌고 깜빡인다.

**판 보기를 누른 동안에는 시간이 흐르지 않는다.** 규칙을 읽느라 시간을 잃지 않게 한 것과 같은 취급이다.

### 판 읽기 — 살펴보기 · 처치 눈금 · 잡은 기물

**아무 기물이나 눌러 사거리 보기.** 내 것이든 상대 것이든, 내 차례든 아니든 누르면
그 기물이 갈 수 있는 칸이 뜬다. 상대 차례를 가정해 계산해야 해서 `turn` 을 잠시 바꿔 뽑는다.
**두는 것과 헷갈리면 안 되므로 색을 다르게** 썼다 — 둘 수 있는 칸은 파랑(primary),
살펴보기는 보라(accent). 같은 기물을 다시 누르면 꺼진다.

**처치 눈금.** 막대 하나에 "다음 N처치" 글자만 있어서 1·3·6·11 이 어디쯤인지 안 보였다.
눈금을 직접 찍고 숫자도 같이 쓴다. 지난 눈금은 파랑, 다음 눈금은 주황.

**잡은 기물** 을 각자 시계 위에 작게 늘어놓는다. 값 큰 것부터 정렬하고, 앞서면 `+8` 처럼 우세를 붙인다.

### 온라인 방 설정 — 시간 · 내 색 · 대국 중단

방을 만들 때 제한시간과 **내 색(백/흑/랜덤)** 을 고른다. 랜덤은 서버가 방을 열 때 정한다.
참가자는 남은 자리에 앉는다.

고치면서 드러난 것: 곳곳에 **"방장 = 백"** 이 박혀 있었다. 방장이 흑을 고르면 판이 아예 안 열렸다.
`Net.isHost` 를 색과 분리해 기록하도록 바꿨다.

**대국 중단** 도 넣었다. 항복과 달리 **승패를 남기지 않는다** — 전적에도 안 들어간다.
온라인이면 상대에게도 전달된다.

### 진영 스트립이 좁아지면 겹치던 문제

스트립 너비는 창이 아니라 **판 너비**에 딸려 있다. 그래서 창 기준 미디어쿼리로는
좁아지는 순간을 못 잡았고, 마지막 수·증강·시계가 서로 겹쳐 찍혔다.
어느 폭에서든 접히도록 `flex-wrap` 을 항상 켜고, 마지막 수 버튼은 줄어들며 말줄임되게 했다.
460px 까지 넘침 없음을 확인.

### 도약 경로는 '평소와 다르게 움직인 수' 에만

보통 나이트 수까지 경로를 그리니 어수선했다.
증강 때문에 평소와 다르게 간 수(`N11b` 의 4·2 도약 등)에만 그린다.
평범한 나이트 수(g1→f3)는 아무것도 안 뜬다.

### B3c — 살아남아야 하는 기간 1턴 → 2턴

한 턴은 너무 쉽게 통과해서 조건이 사실상 없는 것과 같았다.

### 배포 준비

`render.yaml` · `requirements.txt` 추가. 중계 서버(`relay.py`)가 게임 파일까지 함께 내려주므로
**서비스 하나, 주소 하나**로 온라인 대전이 끝난다. 리전은 싱가포르 — 한국에서 가장 가깝고,
실시간 대전이라 지연이 곧 체감이다. 설치할 패키지가 없어 `requirements.txt` 는 비어 있지만,
배포 플랫폼이 파이썬 프로젝트로 알아보도록 둔다.

### 방 참가가 안 되던 문제 — 같은 포트에 서버가 두 개

코드를 입력해도 "그런 방이 없습니다" 가 뜨던 문제.

Windows 의 `SO_REUSEADDR` 는 리눅스와 달리 **이미 듣고 있는 포트에 또 바인드하는 것까지 허용**한다.
그대로 켜 둔 탓에 실행 버튼을 두 번 누른 것만으로 같은 포트에 서버가 둘 떴다.
방은 프로세스 메모리에 있으므로, 방을 만든 사람과 들어오는 사람이 서로 다른 프로세스에 붙으면 방을 찾을 수 없다.

세 겹으로 막았다.

1. `relay.py` — Windows 에서는 `SO_EXCLUSIVEADDRUSE` 로 배타 바인드. 두 번째는 실패하고, 조용히 죽는 대신 이유를 찍는다.
2. `launch.py` 의 빈 포트 탐색 — 같은 옵션을 줘야 '쓰고 있는 포트'를 비었다고 넘겨주지 않는다.
3. `launch.py` — 새로 띄우기 전에 `/healthz` 로 이미 떠 있는 무제체스를 찾고, 있으면 그 주소만 연다.

### 중계 서버를 Node 에서 파이썬으로

다른 컴퓨터에서 온라인 대전이 안 되던 문제.
`node_modules` 는 저장소에 올라가지 않으므로 새 컴퓨터에서 받으면 `ws` 패키지가 없고,
실행기가 정적 서버로 떨어져 온라인이 조용히 꺼져 있었다.

이 게임의 장점은 원래 "설치도 빌드도 필요 없다" 인데 온라인을 붙이면서 그걸 깨뜨린 셈이라,
WebSocket 중계를 **파이썬 표준 라이브러리로 다시 썼다** (`server/relay.py`).
파이썬은 실행기가 이미 쓰고 있어 새로 붙는 의존성이 없다. 프로토콜은 그대로라 클라이언트는 한 줄도 안 바뀌었다.
`server/index.js` · `package.json` 은 지웠다 — 같은 프로토콜을 두 벌 유지하면 반드시 어긋난다.

### AI 탐색 강화

가장 컸던 건 **루트에서 알파베타를 통째로 버리고 있던 것**. 외통수를 놓치던 옛 버그를 피하려고
뿌리의 모든 수를 전체 창으로 봤는데, 그러면 가지치기가 하나도 안 걸린다.
창의 기준선을 난이도 잡음만큼만 내려 좁은 창으로 훑도록 바꿨다 — 잡음으로 뽑힐 범위의 수는 점수가 그대로 정확하다.

그 밖에 널무브 가지치기, 체크 연장, 정지탐색 델타 가지치기, 증분 Zobrist 해시.
전치표의 외통 점수를 ply 로 보정하지 않아 깊이마다 외통 거리가 틀어지던 것도 고쳤다.

| 난이도 | 도달 깊이 (전 → 후) |
|---|---|
| 초급 | 1 → 2 |
| 중급 | 2 → 3.5 |
| 고급 | 3 → 5.5 |

맞대결: 고급 설정 14판 **10승 1패 3무(82%)**, 중급 설정 10판 **7승 2패 1무(75%)**.
난이도별 잡음·실수 확률은 그대로 — 초급은 여전히 일부러 틀린다.

### 배경 장식

허전하던 배경에 모서리 색 번짐, 68px 격자 결, 큰 기물 글리프 6개(4~9% 투명도, 느린 부유)를 넣었다.
색은 전부 accent 램프에서만 — status 색(빨강·초록·주황)은 이 화면에서 이미 뜻이 있어 배경에 쓰면 오독된다.
`prefers-reduced-motion` 이면 흐르지 않는다.

메인에서 배경이 비쳐 보이도록 `#home` 을 반투명으로 뒀는데 밑의 게임 화면까지 비쳐서,
메인에 있는 동안에는 게임 화면을 감춘다(`body.athome`).

### 메인 화면 배치

AI 대전 · 2인 대전 · 온라인 대전을 한 줄에 두고 기록을 그 오른쪽에. 메인 폭 900 → 1180px.
1060px 아래로 좁아지면 기록만 카드 아래로 내려간다.
온라인 전적이 기록에 승패 대신 `win`/`lose` 로 찍히던 것도 고쳤다.

---

## 2026-09-09

### 도약 경로 표시

`N11b`(범위 2배 · 직선 두 칸 + 대각선 두 칸)처럼 건너뛰는 수는 어느 길로 갔는지가 판에 안 남는다.
기물을 고르고 갈 곳에 마우스를 올리면 지나갈 칸에 순번이 찍히고, 두고 나면 3.6초 동안 같은 경로가 뜬다.
**상대 화면에도 똑같이** 뜨고, 마지막 수 버튼으로 언제든 다시 본다.

경로 계산을 `engine.leapPath` 한 곳으로 모았다 — `N6a`(밟고 간 기물 포영)가 L자 경로를 따로 세고 있었는데,
`N11b` 로 범위가 2배가 되면 엉뚱한 칸을 집었다.

### 항복 버튼

헤더에 항복 버튼. AI·2인은 지금 둘 차례가, 온라인은 내가 진다.
온라인은 상대에게도 전달되고 전적에 패배로 남는다. 온라인의 '다시 시작' 은 '재대국 요청' 으로 분리했다.

### 증강 발동 버그 3건

- **포영·지정불가가 안 걸리던 것** — 지속시간이 한 수 모자랐다. 만료 검사는 턴 수를 올린 *뒤* 에 도는데
  만료 시점을 그 앞으로 잡아, "다음 상대턴 동안" 짜리가 실질 0턴이었다.
- **증강을 열어 준 그 처치가 정작 대상에서 빠지던 것** — 처치 훅이 드래프트보다 먼저 돌기 때문.
  `P1c`·`B3c` 등 8종이 조건을 만든 바로 그 수에서 발동하지 않았다.
- **`P1b`(두 번 전진)** 가 이미 움직인 폰에게도 3칸을 주던 것. 이제 1~2칸.

### 도감 · 규칙 화면 재구성

도감은 기물/처치수 선택기와 검색으로 나눠 보게, 규칙은 '증강 얻는 흐름' 4단계와 카드 격자로.
증강 색 기준을 기물 → 지속 유형(턴제한·횟수제한·영구지속)으로 바꿨다.
킹 1개 티어가 4개였던 것(`K1d`)을 빼서 24칸 전부 3개로 되돌렸다.
