# 피드백

받은 글은 **한 글자도 바꾸지 않고** 그대로 두고, 줄 끝에 처리 표시와 짧은 설명만 붙였다.
자세한 내용은 [docs/수정한_내용.md](docs/수정한_내용.md) 에 날짜·시간별로 있다.

- ✅ **고침** — 코드를 바꿨다
- 🔎 **확인** — 돌려 봤더니 이미 정상이라, 다시 깨지지 않게 테스트로 잠갔다

전부 **실제 화면에서** 한 번 더 확인했다 — `python server/relay.py` 를 띄우고 `node tools/test/feedback.js` (48항목).
판만 차려 놓고 그 뒤로는 사람과 같은 길로만 간다: 칸을 눌러서 두고, 드래프트 카드를 누르고, 대상 지정 바에서 칸을 찍는다.

---

## 시작화면

1. 증강 도감, 규칙/용어 기능이 게임 실행 후 나옴 — ✅ **고침**
   > 모달이 메인 화면보다 아래에 깔려 있었다. 지금은 메인 화면 아래 줄의 `증강 도감` · `규칙 · 용어` 로 바로 열린다.
   > (09-16 결함 7 · 화면 확인 09-17 15:58)

## 증강 도감

1. 증강도감 가독성 개선 필요 (애당초 기물별로 볼 수 있게 or 티어별로) — ✅ **고침**
   > 기물 · 처치 수 차림표는 09-09 부터 있었다. 남은 문제는 **카드 높이**였다 — 티어 칼럼을 subgrid 로 묶으면서
   > 행을 `1fr` 로 둬서 짧은 카드까지 제일 긴 카드 높이로 늘어나, 한 화면에 여섯 장밖에 안 들어왔다.
   > 행을 `auto` 로 바꿔 같은 줄끼리만 맞춘다. 이제 한 기물 12장이 한 화면이다. (09-17 13:48)
2. 첫 킹 증강개수 오류 (1개 더 있음) — ✅ **고침**
   > 도감·데이터는 정상이었다(킹은 티어마다 3개, 총 12종). 증강이 하나 더 들어오던 건 **`K1b`** 때문.
   > "그 칸의 3개 중 2개" 인데 구현이 '드래프트를 한 번 더' 라 회차마다 칸을 다시 골랐고,
   > 남은 둘이 다 잠기면 **다른 기물 칸**으로 넘어가 엉뚱한 증강을 하나 더 줬다.
   > 이제 칸은 첫 회차에만 고르고 두 번째는 같은 칸을 다시 펼친다. (09-17 15:45)

## 플레이화면

1. 증강에서 턴제한, 횟수제한, 영구지속 카테고리에 따라 증강색 변경 — 🔎 **확인**
   > 이미 들어가 있다 — 턴제한 파선 · 횟수제한 점선 · 영구지속 실선에 칩과 범례까지.
   > 드래프트 카드 세 장의 색이 실제로 갈리는지 화면에서 확인했다. (09-17 15:58)
2. 증강 선택 중 판 보는 경우, '판을 보는 중입니다' 및 '증강 고르기로' 문구 더 크게 — ✅ **고침**
   > 16px / 14px 이라 판에 묻혔다. 제목 22px · 부제 15px · 버튼 18px 로 키우고 여백도 늘렸다.
   > 실제로 계산된 글씨 크기를 화면에서 재서 확인한다. (09-17 13:44)

## 규칙/용어
1. 규칙/용어 UI 수정 필요 (한눈에 안들어옴) — ✅ **고침**
   > 네 덩이가 세로로 이어져 있어 용어 하나 보려고 한참 굴려야 했다.
   > 도감과 같은 자리에 **바로가기 한 줄**(증강을 얻는 흐름 · 조작 · 전역 룰 · 용어)을 붙였다. (09-17 13:52)
2. 전역 룰 밑 설명은 주석처리 (굳이 유저에게 안보여줘도 됨) — ✅ **고침**
   > 설계 사유(`why`)는 09-09 부터 화면에 안 그린다. 남은 본문도 증강 ID 가 섞인 예외 설명이라
   > **이름만 먼저 보여 주고 접어 뒀다** — 궁금한 줄만 눌러서 편다. (09-17 13:52)
3. 드래프트 방식 잘못 설명돼있는 거 같은데 — ✅ **고침**
   > 처음 잡았던 "티어의 18개 중 무작위 3개" 설명이 남아 있었다.
   > 지금은 "처치를 해낸 기물의 칸(기물×티어)에 있는 선택지 3개를 그대로 펼쳐 1개를 고른다" 로 맞췄고,
   > `K1b` 문구도 "같은 칸을 한 번 더 펼쳐 그 3개 중 2개" 로 고쳤다. (09-14 13:14 · 09-17 15:45)

## 플레이

1. 포영/지정불가 작동안함 — 🔎 **확인**
   > 지금 코드에서는 둘 다 제대로 돈다. 다시 깨지지 않게 두 겹으로 잠갔다.
   > 엔진 회귀 18항목(`node tools/test/engine.js`) — 지정불가는 처치 대상에서 빠지고 자기도 못 움직이며 정해진 ply 에 풀린다.
   > 포영은 칸을 비우고 나갔다가 제자리로 돌아오고, 그 칸을 차지한 기물은 피아 상관없이 제거된다.
   > 화면에서도 — `P1a` 로 찍은 폰에 배지가 붙고 **흑 룩을 눌러도 그 칸이 잡을 칸으로 안 찍힌다**(옆 칸은 찍힌다).
   > `P1c` 로 두 기물이 판에서 사라지고 칩이 뜨며, 그 칸에 들어간 룩을 돌아온 폰이 밀어낸다. (09-17 13:41 · 15:58)
2. 폰이 두번 전진하는 증강 먹을 시, 기본 배치일 경우 1~3칸, 이미 움직인 폰일 경우 1~2칸이어야 하나 1~3칸으로 돼있음 — ✅ **고침**
   > `far = moved ? 2 : 3`. 화면에서도 기본 배치 폰은 점이 `e3 e4 e5`, 이미 움직인 폰은 `g5 g6` 로 찍힌다.
   > 증강 횟수는 3칸 전진에만 깎인다(원래 규칙인 2칸 전진이 증강을 먹으면 안 되니까). (09-17 13:10 · 15:58)
3. 비숍 비밀 (적을 처치한 비숍 살아있는 경우) 작동 로직 오류 (방금 적을 처치한 비숍이 살아있는 경우인데, 실제 구현은 이후 비숍이 적을 처치하고 1턴동안 살아있을 경우 작동함) 추가로 처치된 기물을 스폰위치에 스폰하는 과정에서, 그 자리에 다른 기물이 있다면 선택이 안돼야하는데 선택은 되고 스폰이 불가능하다는 메시지만 뜨는 불쾌한 경험 有 — ✅ **고침**
   > 셋 다 고쳤다. **그 처치** — 증강을 열어 준 처치를 획득 직후 훅에 한 번 더 흘려보내, 조건을 만든 바로 그 비숍부터 지켜본다.
   > **2턴** — 살아남아야 하는 기간을 1턴에서 2턴으로. 처치한 수 뒤로 한 턴을 더 두고, 그 다음 내 차례가 시작될 때 뜬다.
   > **막힌 칸** — 고른 뒤에 '불가' 를 알리지 않고 처음부터 ✕ 로 잠가서 이유와 함께 보여 준다.
   > 화면에서 b1 · g1 을 막아 두면 나이트가 잠긴 채로 오고, 룩을 골라 a1 에 되살아나는 것까지 확인했다. (09-17 15:58)
